/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // à remplir si tu charges des images externes
  },
};

module.exports = nextConfig;